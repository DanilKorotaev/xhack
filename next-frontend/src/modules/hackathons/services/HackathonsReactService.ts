/* eslint-disable react-hooks/rules-of-hooks */
import useLoader from "../../../hooks/useLoader";

export class HackathonsReactService {
  constructor(

  ) {}

  public useHackathon = (hackathonId: number) => {
    // const loaderResult = useLoader({
    //   api: (token) => this.hackathonsRepository.getHackathonById(new GetHackathonByIdRequest(hackathonId), token),
    //   deps: [hackathonId],
    // });
    //
    // return {
    //   isLoading: loaderResult.isLoading,
    //   isLoadingError: loaderResult.isLoadingError,
    //   data: loaderResult.data,
    // };
  };
}
