import React, { useState } from "react";

export interface IMiniGuideProps {

}

export const MiniGuide: React.FC<IMiniGuideProps> = () => (
  <div>
    MiniGuide
  </div>
);

export default MiniGuide;
